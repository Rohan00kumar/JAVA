public class proLinkedList {
    public class LinkedList {
        int val;
        LinkedList next;

        LinkedList() {
        }

        LinkedList(int val) {
            this.val = val;
        }

        LinkedList(int val, LinkedList next) {
            this.val = val;
            this.next = next;
        }
    }


    // nth node from last and delete 

    // public ListNode removeFromEnd(ListNode head, int n) {
        
    // }
}
